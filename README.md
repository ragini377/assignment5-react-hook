In this project:-

A custom hook named useFetch is created.

The hook accepts a URL parameter.

It fetches data from an API using the fetch() function.

It handles -:

 1 Loading state

 2 Error state

3 Data state

A simple React component displays the fetched data.

Tech Used -:

React (Vite)

JavaScript (ES6)

CSS

Fetch API -:

API Used
https://api.escuelajs.co/api/v1/products


